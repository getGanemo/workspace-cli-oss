"""Embedded guide texts for `wsp guide <topic>`."""

GUIDE_TOPICS = {
    "quickstart": "Paste-ready agent prompt for a fresh AWaC workspace (run this first).",
    "init": "How to scaffold a new workspace from scratch.",
    "onboard-product": "End-to-end onboarding for a NEW product: 9 steps, 5 inputs, scaffold-stack + Cat A repos + project_management + devvault + audit + first workspace.",
    "deploy": "How the deploy router + workspace overrides work.",
    "secrets": "How devvault works (catalog + overrides + per-machine vault).",
    "discover": "Quick orientation for an agent dropped into a fresh workspace.",
}
